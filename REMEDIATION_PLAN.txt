================================================================================
          CI/CD FAILURE REMEDIATION PLAN — 2026-07-15
================================================================================

1. TypeScript Build Failure (@proofbridge/trust-runtime)
   - Problem: Module not found in CI despite workspace configuration.
   - Action:
     - Review `turbo.json` build task dependencies.
     - Add explicit `pnpm` workspace linking diagnostic step in CI (list node_modules).
     - Investigate `moduleResolution` in `tsconfig.json` against CI build context.

2. Qodana Failure
   - Problem: Version mismatch (CLI 2024.3.4 vs Linter 2026.1) and missing token.
   - Action:
     - Update `qodana.yaml` to specify a compatible linter version (e.g., 2024.3)
       OR update the Qodana action in workflow.
     - Add `QODANA_TOKEN` to GitHub Secrets.

3. Jekyll/GitHub Pages Build Failure
   - Problem: "Invalid Date" in markdown frontmatter.
   - Action:
     - Scan `docs/governance/adrs/` for invalid YYYY-MM-DD date formats in YAML headers.
     - Correct all invalid datetime formats to valid YAML/Jekyll strings.

4. Infrastructure/Secret Housekeeping (from SECRETS_AUDIT.txt)
   - Action:
     - Delete unused secrets: `HCP_TERRAFORM_VVU_ARCHITECT_TOKEN`, `TFC_AGENT_TOKEN`,
       `VERCEL_AUTOMATION_BYPASS_SECRET`.
     - Add missing CI secrets: `SUPABASE_*`, `DATABASE_URL`.
     - Consolidate Vercel tokens (optional but recommended).

================================================================================
