================================================================================
          GITHUB ACTIONS SECRETS AUDIT — 2026-07-14 23:50 SAST
================================================================================

CONFIGURED SECRETS (8 total):
─────────────────────────────────────────────────────────────────────────────────
Secret Name                            Last Updated    Referenced In Workflow?
─────────────────────────────────────────────────────────────────────────────────
GH_PAT                                 2 months ago    ✅ YES (git push via SSH)
HCP_TERRAFORM_VVU_ARCHITECT_TOKEN      Last month      ❌ NO — NOT referenced
PROOFBRIDGE_LINER_VERCEL               2 months ago    ✅ YES (deploy-vercel.yml)
TFC_AGENT_TOKEN                        Last month      ❌ NO — NOT referenced
VERCEL_AUTOMATION_BYPASS_SECRET        34 min ago      ❌ NO — NOT referenced
VERCEL_ORG_ID                          30 min ago      ✅ YES (ci-cd.yml)
VERCEL_PROJECT_ID                      30 min ago      ✅ YES (ci-cd.yml)
VERCEL_TOKEN                           15 min ago      ✅ YES (ci-cd.yml)

================================================================================
RECOMMENDATION: DELETE THESE 3 UNUSED SECRETS
================================================================================

1. HCP_TERRAFORM_VVU_ARCHITECT_TOKEN
   Reason: Not referenced in any workflow file. Safe to delete.

2. TFC_AGENT_TOKEN
   Reason: Not referenced in any workflow file. Safe to delete.

3. VERCEL_AUTOMATION_BYPASS_SECRET
   Reason: Not referenced in any workflow file. This is a Vercel-side webhook
   secret for skipping Vercel's own automation triggers — not used by your
   GitHub Actions. Safe to delete unless you have a specific Vercel webhook
   integration that uses it.

To delete (Settings > Secrets and variables > Actions > click secret > Delete):

  gh secret delete HCP_TERRAFORM_VVU_ARCHITECT_TOKEN
  gh secret delete TFC_AGENT_TOKEN
  gh secret delete VERCEL_AUTOMATION_BYPASS_SECRET

================================================================================
RECOMMENDATION: KEEP THESE 4 VERCEL SECRETS
================================================================================

VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID — all actively used by
ci-cd.yml Vercel Deploy job. DO NOT delete.

PROOFBRIDGE_LINER_VERCEL — used by deploy-vercel.yml as VERCEL_TOKEN value.
This is a SECOND Vercel token (possibly older). Options:
  a) Keep it (no harm, deploy-vercel.yml still uses it)
  b) Delete it + update deploy-vercel.yml to use secrets.VERCEL_TOKEN instead

GH_PAT — used for git push operations. Keep.

================================================================================
SECRETS REFERENCED BUT NOT CONFIGURED (Missing)
================================================================================

These are referenced in workflows but NOT in your secrets list:

1. QODANA_TOKEN
   Referenced: qodana.yml (line 27, 30)
   Status: Graceful skip (workflow uses || true)
   Action: Add if you want Qodana security scanning enabled
   Note: You have QODANA_CONFIGURATIONS_TOKEN instead — different secret

2. QODANA_CONFIGURATIONS_TOKEN
   Referenced: qodana-config-upload.yml (line 19)
   Status: Configured but NOT in your list
   Action: Verify — may be same as QODANA_TOKEN or separate

3. REVIEW_TOKEN
   Referenced: attestation.yml (line 70)
   Status: Attestation workflow is DISABLED (if: false)
   Action: Not needed until attestation is re-enabled

4. SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_KEY
   Referenced: Multiple workflows
   Status: Likely set as env vars in Vercel, not GitHub secrets
   Action: Add as GitHub secrets if CI needs them

5. DATADOG_API_KEY, DATADOG_APP_KEY
   Referenced: Unknown workflow
   Status: Not configured
   Action: Add if Datadog monitoring is needed

6. CANTON_JSON_API
   Referenced: Unknown workflow
   Status: Not configured
   Action: Verify if still needed

7. CIRCUIT_BREAKER_ADDRESS, CIRCUIT_BREAKER_UPDATER_ADDRESS,
   CIRCUIT_BREAKER_UPDATER_KEY
   Referenced: Unknown workflow
   Status: Not configured
   Action: Add if contract deployment CI is needed

8. POLYGON_AMOY_RPC
   Referenced: Unknown workflow
   Status: Not configured
   Action: Add if contract CI needs RPC access

9. FROST_GROUP_KEY, FROST_SHARE_1_HEX
   Referenced: Unknown workflow
   Status: Not configured
   Action: Verify if threshold signing is needed in CI

================================================================================
PROOFBRIDGE_LINER_VERCEL — CLEANUP OPTION
================================================================================

This secret is only used in deploy-vercel.yml:
  VERCEL_TOKEN: ${{ secrets.PROOFBRIDGE_LINER_VERCEL }}

If you want to consolidate to a single VERCEL_TOKEN secret:

1. Update deploy-vercel.yml line 24:
   Change: VERCEL_TOKEN: ${{ secrets.PROOFBRIDGE_LINER_VERCEL }}
   To:     VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}

2. Then delete: gh secret delete PROOFBRIDGE_LINER_VERCEL

This consolidates Vercel auth to one secret managed in one place.

================================================================================
SUMMARY
================================================================================

DELETE (3):  HCP_TERRAFORM_VVU_ARCHITECT_TOKEN, TFC_AGENT_TOKEN,
             VERCEL_AUTOMATION_BYPASS_SECRET

KEEP (4):    GH_PAT, VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID

OPTIONAL DELETE (1):  PROOFBRIDGE_LINER_VERCEL (after migrating deploy-vercel.yml)

VERIFY (1):  QODANA_CONFIGURATIONS_TOKEN vs QODANA_TOKEN — are these the same?

MISSING (7+): QODANA_TOKEN, REVIEW_TOKEN, SUPABASE_*, DATADOG_*,
              CANTON_JSON_API, CIRCUIT_BREAKER_*, POLYGON_AMOY_RPC, FROST_*

================================================================================
