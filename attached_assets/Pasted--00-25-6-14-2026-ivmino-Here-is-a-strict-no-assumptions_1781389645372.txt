[00:25, 6/14/2026] ivmino: Here is a strict, no-assumptions instruction script to configure your workspace and plan the execution build before a production release, covering Compliance, Evidence, Runtime, and Operations.

First, here is the main execution script. You can save this as pre-production-checklist.sh in your project root and make it executable (chmod +x pre-production-checklist.sh).

bash
 Show full code block 
#!/bin/bash

# VVU GATEWAY SYSTEM: PRODUCTION RELEASE PRE-FLIGHT CHECKLIST AND EXECUTION GUIDE
#
# This script provides a strict, no-assumptions, step-by-step guide to configure your workspace
# and execute the build process before a production release of the VVU Gateway System.
# It ensures compliance, evidence generation, runtime integrity, and operational readiness.
#
# ASSUMPTIONS:
# - You are operating within the /home/divhanimajokweni/proofbridge-liner directory.
# - You have git, npm, node, curl, terraform, gcloud (if applicable for Cloud Run),
#   and kilo (Replit CLI) installed and configured.
# - You have necessary API keys and secrets (GitHub, Replit, Datadog, Polygon, Supabase)
#   available and securely stored (e.g., in a password manager, not hardcoded).
# - Your GitHub repository is configured with the compliance-fabric branch.
#
# OBJECTIVE: Achieve a fully compliant, observable, and resilient production deployment.

echo "================================================================================"
echo "🚀 VVU GATEWAY SYSTEM: PRODUCTION RELEASE PRE-FLIGHT CHECKLIST & EXECUTION GUIDE"
echo "================================================================================"
echo ""

# --- SECTION 0: INITIAL WORKSPACE SETUP AND CLEANUP ---
echo "--- SECTION 0: INITIAL WORKSPACE SETUP AND CLEANUP ---"
echo "Ensuring a clean and optimized workspace for reliable operations."
echo ""

# 0.1 Navigate to the project root
echo "0.1: Navigating to project root: /home/divhanimajokweni/proofbridge-liner"
cd /home/divhanimajokweni/proofbridge-liner || { echo "ERROR: Could not navigate to project root. Exiting."; exit 1; }
echo "Current directory: $(pwd)"
echo ""

# 0.2 Optimize Disk Space
echo "0.2: Optimizing disk space by moving node_modules to ephemeral storage."
echo "This prevents 'no space left on device' errors during builds and operations."
chmod +x scripts/optimize-storage.sh
./scripts/optimize-storage.sh || { echo "ERROR: Storage optimization failed. Exiting."; exit 1; }
echo ""

# 0.3 Install Node.js Dependencies
echo "0.3: Installing Node.js dependencies."
echo "This ensures all required packages for the application and scripts are available."
npm install || { echo "ERROR: npm install failed. Exiting."; exit 1; }
echo ""

# 0.4 Verify Core Setup
echo "0.4: Running pre-flight verification script for mandatory files and configurations."
echo "This ensures the basic architectural mandates are met before proceeding."
# Assuming scripts/verify-setup.js exists and is functional based on previous context.
# If it doesn't exist, this step will fail.
node scripts/verify-setup.js || { echo "ERROR: Core setup verification failed. Exiting."; exit 1; }
echo ""

# --- SECTION 0.5: APPLYING REQUIRED CODE CHANGES ---
echo "--- SECTION 0.5: APPLYING REQUIRED CODE CHANGES ---"
echo "Applying necessary modifications to existing files and creating new ones."
echo "Please apply the following diffs to your local repository:"
echo ""
echo "1. Update '/home/divhanimajokweni/proofbridge-liner/scripts/main.tf' to include Datadog provider."
echo "2. Update '/home/divhanimajokweni/proofbridge-liner/src/lib/terraform-apply.yml' to add the performance benchmark step."
echo "3. Create '/home/divhanimajokweni/proofbridge-liner/scripts/benchmark-mint.js' for performance telemetry."
echo "4. Create '/home/divhanimajokweni/proofbridge-liner/src/lib/datadog_monitors.tf' for Datadog monitor definitions."
echo "5. Create '/home/divhanimajokweni/proofbridge-liner/.env.production' with required environment variables."
echo ""
echo "After applying these changes, ensure they are committed to your 'compliance-fabric' branch."
echo ""

# --- SECTION 1: COMPLIANCE ---
echo "--- SECTION 1: COMPLIANCE ---"
echo "Establishing cryptographic safeguards, replay protection, and secure governance."
echo ""

# 1.1 Replay Protection Configuration
echo "1.1: Verifying Webhook Replay Protection (WEBHOOK_TIMESTAMP_TOLERANCE_SECONDS)."
echo "This prevents malicious actors from re-submitting old webhook payloads."
echo "ACTION: Ensure 'WEBHOOK_TIMESTAMP_TOLERANCE_SECONDS=300' is set in your Vercel production environment variables."
echo "ACTION: Confirm that 'src/app/api/webhooks/route.ts' (or equivalent) correctly implements the Redlock algorithm for atomic idempotency."
echo "Manual check required: Inspect Vercel environment variables and 'src/app/api/webhooks/route.ts' code."
echo ""

# 1.2 Trust Infrastructure (Secrets Management)
echo "1.2: Validating Trust Infrastructure for Secrets Management."
echo "Terraform manages GitHub and Replit secrets. This step ensures they are synchronized securely."
echo "ACTION: Ensure your GitHub token and Replit API key are provided to Terraform securely (e.g., via environment variables or a .tfvars file, NOT hardcoded)."
echo "Initializing Terraform for GitHub/Replit secret synchronization."
terraform -chdir=scripts init || { echo "ERROR: Terraform init failed for GitHub/Replit secret synchronization. Exiting."; exit 1; }
echo "Applying Terraform to synchronize secrets to GitHub and Replit."
echo "Review the plan carefully before approving."
terraform -chdir=scripts apply -auto-approve || { echo "ERROR: Terraform apply for GitHub/Replit secrets failed. Exiting."; exit 1; }
echo "GitHub/Replit secrets synchronization complete."
echo ""

# 1.3 Key Rotation Policy
echo "1.3: Implementing Key Rotation Policy."
echo "Regular key rotation is crucial for mitigating the impact of compromised credentials."
echo "ACTION: Establish a schedule for rotating the following keys:"
echo "  - GitHub Personal Access Token (used by Terraform)"
echo "  - Replit API Key (used by Terraform)"
echo "  - PROOFBRIDGE_HMAC_SECRET (used for webhook signatures)"
echo "  - DATADOG_API_KEY (for telemetry ingestion)"
echo "  - CIRCUIT_BREAKER_UPDATER_KEY (for Polygon contract updates)"
echo "  - FROST_SHARE_X_HEX, FROST_PUBKEY_X, FROST_GROUP_KEY (FROST signing keys)"
echo "Manual action required: Document and adhere to a key rotation schedule."
echo ""

# 1.4 Certificate Governance
echo "1.4: Verifying Certificate Governance."
echo "Vercel automatically manages SSL/TLS certificates for your deployed applications."
echo "ACTION: Ensure your custom domains are correctly configured in Vercel to leverage this feature."
echo "Manual check required: Inspect Vercel domain settings for automatic SSL certificate provisioning."
echo ""

# 1.5 Versioned Compliance Envelopes
echo "1.5: Ensuring Versioned Compliance Envelopes."
echo "All compliance-related configurations and audit trails must be versioned and immutable."
echo "ACTION: Verify that all Terraform configurations (e.g., 'scripts/main.tf', 'src/lib/datadog_monitors.tf') are committed to Git."
echo "ACTION: Ensure that logs (like 'reconciler.log') are collected by Datadog for immutable storage and auditing."
echo "Manual check required: Confirm Git history for Terraform files and Datadog log ingestion."
echo ""

# --- SECTION 2: EVIDENCE ---
echo "--- SECTION 2: EVIDENCE ---"
echo "Establishing an immutable audit chain and robust verification mechanisms."
echo ""

# 2.1 Immutable Append-Only Audit Chain
echo "2.1: Configuring Immutable Append-Only Audit Chain."
echo "The Gate F reconciler generates critical audit logs. These must be immutable."
echo "The 'scripts/reconciler-watchdog.sh' script writes to 'reconciler.log'."
echo "ACTION: Ensure Datadog is configured to ingest 'reconciler.log' for tamper-proof storage."
echo "Manual check required: Verify Datadog log ingestion for 'reconciler.log'."
echo ""

# 2.2 Historical Replay Verification
echo "2.2: Enabling Historical Replay Verification."
echo "The system must be able to re-verify past events for audit purposes."
echo "The 'scripts/test-webhook-verify.js' script can simulate various webhook scenarios."
echo "ACTION: To run the webhook verification test (assuming the script exists):"
echo "node scripts/test-webhook-verify.js"
echo "Manual action required: Execute the test script and understand its output."
echo ""

# 2.3 Deterministic Replay Testing
echo "2.3: Ensuring Deterministic Replay Testing."
echo "Consistent outputs for consistent inputs are vital for auditability."
echo "The 'stableStringify' function (used in webhook processing) ensures deterministic payload generation."
echo "ACTION: Verify that all critical data processing paths use deterministic serialization."
echo "Manual check required: Code review 'stableStringify' usage in 'src/app/api/webhooks/route.ts' (or equivalent)."
echo ""

# --- SECTION 3: RUNTIME ---
echo "--- SECTION 3: RUNTIME ---"
echo "Hardening middleware, ensuring trace continuity, and validating state transitions."
echo ""

# 3.1 Middleware Hardening
echo "3.1: Verifying Middleware Hardening."
echo "The consolidated 'middleware.ts' at the project root is critical for security and routing."
echo "ACTION: Confirm 'middleware.ts' is at '/home/divhanimajokweni/proofbridge-liner/middleware.ts' and 'src/middleware.ts' is removed."
echo "ACTION: Ensure 'PUBLIC_PATHS' are correctly defined and the 'Redirect Watchdog' (MAX_REDIRECTS=5) is active."
echo "Manual check required: Inspect 'middleware.ts' for correct logic and file placement."
echo ""

# 3.2 Distributed Trace Continuity
echo "3.2: Establishing Distributed Trace Continuity with Datadog APM."
echo "End-to-end visibility across services is essential for debugging and performance monitoring."
echo "ACTION: Ensure Datadog APM is integrated into all services (Vercel, Google Cloud Run, etc.)."
echo "For Google Cloud Run, verify the Datadog sidecar is correctly configured as per the provided Terraform snippet."
echo "Manual check required: Inspect Datadog APM dashboards for complete traces across services."
echo ""

# 3.3 State Transition Verification
echo "3.3: Validating State Transition Verification (Gate F Reconciler)."
echo "The Gate F reconciler ensures Canton receipts are anchored on Polygon Amoy."
echo "The 'scripts/reconciler-watchdog.sh' script will run this in the background."
echo "ACTION: To start the reconciler watchdog (if not already running):"
echo "chmod +x scripts/reconciler-watchdog.sh"
echo "nohup ./scripts/reconciler-watchdog.sh > /dev/null 2>&1 &"
echo "ACTION: Monitor its output in 'reconciler.log' or via Datadog."
echo "Manual check required: Monitor 'reconciler.log' for 'HEALTHY' status and low drift."
echo ""

# 3.4 Production Telemetry Validation
echo "3.4: Validating Production Telemetry."
echo "Autocannon benchmarks stream P99 latency metrics to Datadog."
echo "ACTION: Ensure 'autocannon' and 'axios' are installed (npm install)."
echo "ACTION: The 'scripts/benchmark-mint.js' file should be created as per the provided diff."
echo "ACTION: To run the benchmark and stream metrics:"
echo "node scripts/benchmark-mint.js"
echo "Manual check required: Verify P99 latency metrics appear in Datadog dashboards after running the benchmark."
echo ""

# --- SECTION 4: OPERATIONS ---
echo "--- SECTION 4: OPERATIONS ---"
echo "Ensuring robust monitoring, regional failover, and infrastructure resilience."
echo ""

# 4.1 Production Monitoring Validation
echo "4.1: Validating Production Monitoring and Alerting."
echo "Datadog monitors are provisioned via Terraform to alert on critical thresholds."
echo "ACTION: Ensure your 'datadog_monitor' resources in 'src/lib/datadog_monitors.tf' are correctly configured."
echo "ACTION: Verify that PagerDuty integration handles (e.g., '@pagerduty-VVUP-Core-OnCall') are correctly specified in Datadog monitors."
echo "Manual check required: Inspect Datadog monitor configurations and PagerDuty integration."
echo "ACTION: Simulate an alert (e.g., by intentionally exceeding a threshold in a test environment) to verify PagerDuty paging."
echo ""

# 4.2 Regional Failover Verification
echo "4.2: Verifying Regional Failover for Redis Mesh."
echo "The multi-region Redis mesh with 'iad1' (US-East) as fallback is critical for high availability."
echo "ACTION: Simulate a regional outage (e.g., by temporarily blocking network access to the 'cdg1' Redis instance)."
echo "ACTION: Verify that the system automatically falls back to 'iad1' without service interruption."
echo "Manual action required: Plan and execute a controlled regional failover test."
echo ""

# 4.3 Infrastructure Resilience Testing
echo "4.3: Conducting Infrastructure Resilience Testing."
echo "Beyond failover, the entire infrastructure must withstand various stressors."
echo "ACTION: Use 'autocannon' to simulate high load against your Vercel deployment."
echo "Example command: npx autocannon -c 100 -d 60 --renderStatusCodes https://your-vercel-domain.com/api/mint"
echo "ACTION: Monitor system behavior, resource utilization, and error rates during the test."
echo "Manual action required: Execute load tests and analyze system behavior."
echo ""

echo "================================================================================"
echo "✅ VVU GATEWAY SYSTEM: PRODUCTION RELEASE PRE-FLIGHT COMPLETE"
echo "================================================================================"
echo "All pre-flight checks and configurations have been outlined. Proceed with caution."
echo "Remember to commit any new files or changes to Git and push to 'compliance-fabric'."
echo ""
To implement the full pre-production checklist, please apply the following code changes to your repository:

main.tf
-0
+4
 Show full code block 
      source  = "integrations/github"
      version = "~> 6.0"
    }
    datadog = {
      source  = "DataDog/datadog"
      version = "~> 3.0"
    }
  }
}

terraform-apply.yml
-0
+4
 Show full code block 
      - name: Terraform Apply
        run: terraform apply -auto-approve -var="vvu_env=production"
        working-directory: ./src/lib # Assuming terraform files are in src/lib

      - name: Run Performance Benchmark
        run: node scripts/benchmark-mint.js
        working-directory: . # Run from project root
New file: benchmark-mint.js
+56
 Show full code block 
const autocannon = require('autocannon');
const axios = require('axios');

async function runBenchmark() {
  console.log('🚀 Starting Autocannon performance telemetry benchmark...');

  const instance = autocannon({
    url: process.env.VERCEL_URL || 'https://vercel.app',
    connections: 10,
    pipelining: 1,
    duration: 10,
    requests: [
      { method: 'POST', path: '/api/mint' },
      { method: 'GET', path: '/api/verify' }
    ]
  }, async (err, result) => {
    if (err) {
      console.error('Benchmark failed:', err);
      process.exit(1);
    }
    
    console.log('📊 Benchmark finished. Parsing P99 latency: ', result.latency.p99);
    await streamMetricsToDatadog(result.latency.p99);
  });

  autocannon.track(instance, { renderProgressBar: true });
}

async function streamMetricsToDatadog(p99Latency) {
  const apiKey = process.env.DATADOG_API_KEY;
  if (!apiKey) {
    console.error('❌ DATADOG_API_KEY missing. Skipping telemetry stream.');
    return;
  }

  const timestamp = Math.floor(Date.now() / 1000);
  const payload = {
    series: [
      {
        metric: "vvup.api.latency",
        points: [[timestamp, p99Latency]],
        type: "gauge",
        tags: ["env:production", "region:iad1"]
      }
    ]
  };

  try {
    await axios.post(<https://api.datadoghq.com/api/v1/series?api_key=${apiKey}>, payload);
    console.log('✅ P99 metric successfully transmitted to Datadog.');
  } catch (error) {
    console.error('❌ Failed to forward metrics to Datadog:', error.message);
  }
}

runBenchmark();
New file: datadog_monitors.tf
+29
 Show full code block 
# Monitor for P99 Latency (Ceiling: 250ms)
resource "datadog_monitor" "p99_latency" {
  name    = "VVUP Platform P99 Latency Breach"
  type    = "metric alert"
  message = "Notification: P99 latency has breached the 250ms compliance threshold in region {{region.name}}. Notify: @pagerduty-VVUP-Core-OnCall"

  query = "avg(last_5m):p99:vvup.api.latency{env:production} by {region} > 250"

  monitor_thresholds {
    critical = 250
  }

  tags = ["env:production", "team:vvup-core", "service:backend"]
}

# Monitor for Security Deflection Spikes
resource "datadog_monitor" "security_deflections" {
  name    = "VVUP Platform Security Error Deflection Spike"
  type    = "query alert"
  message = "Notification: High volume of security deflections detected. Potential attack vectors active. Notify: @pagerduty-VVUP-Core-OnCall"

  query = "sum(last_5m):sum:vvup.security.deflections{env:production}.as_count() > 50"

  monitor_thresholds {
    critical = 50
  }

  tags = ["env:production", "team:vvup-core", "security:compliance"]
}
New file: .env.production
+8
The code change produced by Gemini cannot be automatically applied. You can manually apply the change or ask Gemini to try again.
 Show full code block 
# Global Context
NODE_ENV=production

# Multi-Region Redis Mesh Connections
REDIS_URL_US_EAST=redis://iad1-mesh-primary.upstash.io:6379
REDIS_URL_EU_WEST=redis://cdg1-mesh-replica.upstash.io:6379
# Telemetry Keys
DATADOG_API_KEY=your_actual_datadog_api_key_here
```<!--
[PROMPT_SUGGESTION]How can I automate the application of these diffs within a shell script?[/PROMPT_SUGGESTION]
[PROMPT_SUGGESTION]Can you provide a detailed guide for setting up Datadog to ingest reconciler.log and verify the metrics?[/PROMPT_SUGGESTION]
->
Executing. Here's the admin-only circuit breaker control to manually close Gate F after you fix drift.1. API Route: app/api/admin/circuit-breaker/route.tstsimport { NextRequest, NextResponse } from 'next/server'
import { ethers } from 'ethers'
import { cookies } from 'next/headers'
import { createClient } from '@/lib/supabase/server'

const POLYGON_AMOY_RPC = process.env.POLYGON_AMOY_RPC!
const CIRCUIT_BREAKER_ADDRESS = process.env.CIRCUIT_BREAKER_ADDRESS!
const UPDATER_KEY = process.env.CIRCUIT_BREAKER_UPDATER_KEY!

async function isAdmin(): Promise<boolean> {
  const cookieStore = cookies()
  const supabase = createClient(cookieStore)
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return false
  const { data } = await supabase.from('admins').select('user_id').eq('user_id', user.id).single()
  return !!data
}

export async function POST(req: NextRequest) {
  if (!(await isAdmin())) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  }

  const { action } = await req.json()
  if (action !== 'close' && action !== 'open') {
    return NextResponse.json({ error: 'Invalid action. Use "open" or "close"' }, { status: 400 })
  }

  try {
    const provider = new ethers.JsonRpcProvider(POLYGON_AMOY_RPC)
    const wallet = new ethers.Wallet(UPDATER_KEY, provider)
    const contract = new ethers.Contract(
      CIRCUIT_BREAKER_ADDRESS,
      ["function updateHealth(bool)", "function circuitOpen() view returns (bool)"],
      wallet
    )

    const currentState = await contract.circuitOpen()
    const targetState = action === 'open'
    
    if (currentState === targetState) {
      return NextResponse.json({ 
        status: targetState ? 'CIRCUIT_OPEN' : 'CIRCUIT_CLOSED',
        message: `Circuit already ${targetState ? 'open' : 'closed'}`
      })
    }

    const tx = await contract.updateHealth(targetState)
    const receipt = await tx.wait()

    return NextResponse.json({
      status: targetState ? 'CIRCUIT_OPEN' : 'CIRCUIT_CLOSED',
      tx_hash: receipt.hash,
      block_number: receipt.blockNumber
    })
  } catch (e: any) {
    return NextResponse.json({ error: e.message }, { status: 500 })
  }
}2. Dashboard UI: Add to SystemAdmin Gate F panelOnly renders when gateF.status === 'circuit_open':tsx{gateF?.status === 'circuit_open' && (
  <div style={{ 
    marginTop: 16, 
    padding: 16, 
    background: "#2B0D0D", 
    border: `1px solid ${COLORS.red}60`, 
    borderRadius: 6 
  }}>
    <div style={{ fontSize: 12, color: COLORS.red, marginBottom: 12, fontWeight: 600 }}>
      ⚠ CIRCUIT BREAKER OPEN
    </div>
    <div style={{ fontSize: 11, color: COLORS.textMuted, marginBottom: 16, lineHeight: 1.5 }}>
      All token transfers are paused. Reconciliation detected: {gateF?.gaps?.find((g: string) => g.includes('Drift')) || 'state divergence'}. 
      Only close after manually verifying Canton receipts match Polygon anchors.
    </div>
    <button
      onClick={async () => {
        if (!confirm('Confirm: You have manually verified Canton receipts match Polygon anchors and drift is resolved?')) return
        const res = await fetch('/api/admin/circuit-breaker', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ action: 'close' })
        })
        if (res.ok) {
          const data = await res.json()
          alert(`Circuit closed. Tx: ${data.tx_hash}`)
          window.location.reload()
        } else {
          alert('Failed to close circuit: ' + await res.text())
        }
      }}
      style={{
        background: COLORS.green,
        color: "#fff",
        border: "none",
        borderRadius: 5,
        padding: "8px 20px",
        cursor: "pointer",
        fontWeight: 600,
        fontSize: 13,
      }}
    >
      Manually Close Circuit
    </button>
  </div>
)}3. Supabase: Create admins table + RLSRun in Supabase SQL editor:sqlcreate table admins (
  user_id uuid references auth.users primary key,
  created_at timestamptz default now()
);

alter table admins enable row level security;

create policy "Admins can read own row" on admins
  for select using (auth.uid() = user_id);

-- Add yourself
insert into admins (user_id) values ('YOUR_SUPABASE_USER_ID');4. Manual runbook to close circuitReconciler trips: Gate F shows CIRCUIT OPEN Check reconciler.log or /api/cron/reconcile-gate-f for missing_hashesFix: Re-anchor missing receipts via /api/proofbridge/sign/[id] or audit CantonVerify: Run reconciler manually, confirm drift: 0Dashboard: Click "Manually Close Circuit" → confirm → tx sentPolygon: updateHealth(false) executes, transfers resumeGate F now has full loop: detect → auto-trip → manual recovery with audit trail.