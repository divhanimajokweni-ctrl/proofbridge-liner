# ── Service (sync wave 0) ──
apiVersion: v1
kind: Service
metadata:
  name: vvu-dashboard
  namespace: vvu-dashboard
  labels:
    app.kubernetes.io/name: vvu-dashboard
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vvu-dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
