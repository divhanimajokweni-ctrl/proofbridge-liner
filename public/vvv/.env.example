# ProofBridge Liner Environment Configuration
# Copy to .env.local or set in Vercel dashboard

# Secret key for HMAC signatures on kernel outputs
# Generate: openssl rand -hex 32
KERNEL_SECRET=your-secret-key-change-this-in-production

# Optional: override base threshold (default 0.6)
# BASE_THRESHOLD=0.6