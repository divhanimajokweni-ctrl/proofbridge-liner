[package]
name = "threshold_merkle"
type = "bin"
authors = ["ProofBridge Engine"]
compiler_version = ">=0.30.0"

[dependencies]
poseidon = { tag = "v0.1.0", git = "https://github.com/noir-lang/poseidon" }
