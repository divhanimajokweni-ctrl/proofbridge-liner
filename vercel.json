{
  "framework": "nextjs",
  "installCommand": "bun install",
  "buildCommand": "DATABASE_URL=file:./build-tmp.db bunx prisma generate && DATABASE_URL=file:./build-tmp.db bun run build",
  "regions": ["iad1"],
  "env": {
    "COMPUTE_PROVIDER": "local",
    "NEXT_TELEMETRY_DISABLED": "1"
  }
}
