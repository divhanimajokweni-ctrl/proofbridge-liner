{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "DATABASE_URL=file:./build-tmp.db npx prisma generate && DATABASE_URL=file:./build-tmp.db npx next build",
  "regions": ["iad1"],
  "env": {
    "COMPUTE_PROVIDER": "local",
    "NEXT_TELEMETRY_DISABLED": "1",
    "NEXT_IGNORE_TS": "1",
    "DATABASE_URL": "file:./build-tmp.db"
  }
}
