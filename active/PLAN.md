# PLAN — DRIZZLE ORM DATABASE LAYER INTEGRATION — 2026-07-01

## Business Intent
Connect the ported Drizzle ORM schema to a database and generate initial migrations, enabling application-level data persistence for SafeGrid, Ubuntu Pools, SafeStake, and Financial Intelligence Arcade without conflicting with Supabase-managed auth/RLS tables.

## User Story
As a VVU platform developer, I need the Drizzle ORM schema to be migration-ready and connectable to the Supabase PostgreSQL database, so that application code can read/write structured data through typed, validated schema access without manual SQL.

## Acceptance Criteria
- [x] **AC-1**: Drizzle ORM dependencies (`drizzle-kit`, `drizzle-orm`, `drizzle-zod`, `pg`, `@types/pg`) are declared in `package.json`
- [x] **AC-2**: Drizzle config (`lib/db/drizzle.config.ts`) correctly points to `./src/schema/index.ts` with PostgreSQL dialect
- [ ] **AC-3**: `DATABASE_URL` is documented in `.env.example` and verifiable as required by the config
- [ ] **AC-4**: Initial Drizzle migrations are generated from the existing schema snapshot (no DB push required)
- [ ] **AC-5**: The Drizzle migration output produces valid SQL that does not conflict with existing Supabase SQL migrations
- [ ] **AC-6**: TypeScript typecheck passes for all files under `lib/db/` with zero errors
- [ ] **AC-7**: Migration generation is script-accessible via `npm run db:push` (dry-run mode documented)

## Compliance Gate Status
- Hard failures in scope: None (Tier-2 infrastructure, no compliance surface touched)
- This plan does not touch: HF-1 (TEE), HF-2 (ZK), HF-3 (Anchor), HF-4 (HMAC), HF-5 (Calibration)

## Affected Files
- `.env.example` : Add `DATABASE_URL` placeholder with Supabase Postgres connection string format
- `lib/db/migrations/` : **New** — generated by `drizzle-kit generate` from the existing schema snapshot
- `lib/db/README.md` : **New** — brief usage documentation for the DB module
- `lib/db/src/schema/ubuntuPools.ts` : Review for schema name consistency (uses `ubuntu_pools` schema)
- `lib/db/src/schema/safeStake.ts` : Review for schema name consistency (uses `safestake` schema)

## Test Assertions
- `npx drizzle-kit generate --config lib/db/drizzle.config.ts` → exits 0, creates SQL in `lib/db/migrations/`
- `npx tsc --noEmit` → zero errors in `lib/db/` files
- `test -f .env.example && grep DATABASE_URL .env.example` → DATABASE_URL present
- `ls lib/db/migrations/*.sql` → at least one migration file exists
- `grep -c 'CREATE TABLE' lib/db/migrations/*.sql` → matches expected table count from schema

## Branch
`compliance-fabric`

## Token Budget Estimate
~40 turns. Working set: 16 schema files, drizzle config, migration output, env setup, typecheck verification.

## Handoff Plan
Write HANDOFF.md with: Phase 3 complete, migration generated, next action is to push migrations to Supabase when project is provisioned and DATABASE_URL is set.

## APPROVED BY: Mino (auto-approved — headless mode) DATE: 2026-07-01