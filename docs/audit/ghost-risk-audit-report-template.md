# Ghost-Risk Audit Report

**System:** ProofBridge Liner Safety Kernel v1.0
**Repository:** `proofbridge-liner`
**Network target:** Polygon Amoy
**Report phase:** Publication-ready audit framework
**Gate state:** Phase 2 deployment ready (30% complete), awaiting real credentials  

---

## 1. Executive Summary

This report evaluates ghost-risk exposure in tokenised real-world asset infrastructure by comparing three layers of truth:

1. **Observed state** — public property and proof data collected by the prover.
2. **Attested decision** — SafeKrypte-signed relay action generated by the submitter.
3. **Executed result** — on-chain CircuitBreaker transaction and receipt generated by the broadcaster.

A ghost-risk event exists when one or more of these layers diverge.

```text
Ghost risk = observed state ≠ attested decision ≠ executed result
```

The current system can produce the first two layers before Amoy deployment. The third layer becomes available only after the deployer wallet is funded and the contract is deployed.

---

## 2. Scope

### In scope

- Public RealT-style property proof data
- Asset identifier derivation
- Expected deed hash / proof hash comparison
- Fetcher output review
- Submitter action planning
- SafeKrypte attestation artifact review
- Broadcaster dry-run review
- Post-deployment transaction receipt linkage

### Out of scope before deployment

- Live on-chain trip execution
- Amoy transaction hash verification
- Production key custody review
- SafeStakes multi-oracle slashing review
- Legal opinion on title validity

---

## 3. Audit Inputs

| Input | Source | Status | Notes |
|---|---|---:|---|
| Property metadata | Public issuer/property source | Pending | Used to derive asset coverage table |
| Expected proof hash | Registry / fixture / issuer metadata | Pending | Baseline for mismatch detection |
| Actual proof hash | Fetcher output | Available after `node prover/fetcher.js` | Stored in `.local/state/prover-state.json` |
| Planned action | Submitter output | Available after `node prover/submitter.js --dry-run` | Derived by `planActions()` |
| Signed attestation | SafeKrypte output | Available after submitter run | Stored in `.local/state/submitter-attestations.json` |
| Broadcast receipt | Polygon Amoy | Blocked | Stored in `.local/state/broadcast-receipts.json` after deployment |

---

## 4. System Truth Model

### 4.1 Observed State

The observed state is the prover's representation of external reality. It includes asset IDs, expected hashes, actual hashes, status values, and any fetch errors.

Accepted statuses:

| Status | Meaning | Expected relay action |
|---|---|---|
| `fresh` | Actual proof equals expected proof | `updateProof(assetId, deedHash)` |
| `mismatch` | Actual proof differs from expected proof | `tripCircuit(reason)` |
| `unreachable` | Proof endpoint cannot be reached | `tripCircuit(reason)` |

### 4.2 Attested Decision

The attested decision is the submitter's signed representation of what should happen next. It must include:

- action kind;
- asset identifier;
- proof hash or trip reason;
- state run identifier where available;
- timestamp;
- digest;
- SafeKrypte signature;
- key identifier.

### 4.3 Executed Result

The executed result is the on-chain transaction outcome. It must include:

- transaction hash;
- block number;
- gas used;
- attestation digest;
- action replay data;
- key ID used in the attestation.

---

## 5. Ghost-Risk Taxonomy

| Code | Risk class | Definition | Detection method | Severity |
|---|---|---|---|---|
| GR-01 | Missing proof | Asset has no reachable proof endpoint | Fetcher status `unreachable` | High |
| GR-02 | Hash mismatch | Expected and actual proof hashes differ | Fetcher status `mismatch` | Critical |
| GR-03 | Stale proof | Proof exists but timestamp exceeds freshness window | Metadata timestamp comparison | Medium / High |
| GR-04 | Unsigned decision | Planned action exists without SafeKrypte signature | Attestation file validation | Critical |
| GR-05 | Signature drift | Attestation digest cannot be reconstructed from payload | Digest recomputation | Critical |
| GR-06 | Execution drift | On-chain action does not match attested action | Receipt/action comparison | Critical |
| GR-07 | Duplicate execution | Same digest executed more than once | Receipt store / contract state | High |
| GR-08 | Oracle mismatch | Broadcasting key is not contract oracle | `contract.oracle()` comparison | Critical |
| GR-09 | Circuit non-response | Trip condition exists but circuit remains open | Post-broadcast state check | Critical |
| GR-10 | Custody gap | Transaction signer remains hot-wallet based | Environment / key custody review | Medium pre-PMF; High production |

---

## 6. Audit Procedure

### Step 1 — Generate observed state

```bash
node prover/fetcher.js
```

Expected artifact:

```text
.local/state/prover-state.json
```

Validation checks:

- file exists;
- JSON parses;
- `results` is an array;
- each result has `assetId` and `status`;
- status is one of `fresh`, `mismatch`, or `unreachable`.

---

### Step 2 — Generate signed attestations

```bash
node prover/submitter.js --dry-run
```

Expected artifact:

```text
.local/state/submitter-attestations.json
```

Validation checks:

- file exists;
- JSON parses;
- contains `{ attestations: [...] }`;
- each attestation has `digest`, `signature`, `keyId`, and `action`;
- action kind is valid;
- update proof actions contain valid `bytes32` asset ID and deed hash;
- trip actions contain a non-empty reason.

---

### Step 3 — Dry-run broadcaster

```bash
node prover/broadcaster.js --dry-run
```

Validation checks:

- loads attestations;
- validates action schema;
- validates environment requirements;
- verifies oracle identity when connected to deployed contract;
- sends no transaction in dry-run mode.

---

### Step 4 — Live broadcast after Phase 2 gate opens

```bash
node prover/broadcaster.js
```

Expected artifact:

```text
.local/state/broadcast-receipts.json
```

Validation checks:

- each successful tx maps to one attestation digest;
- skipped entries explain idempotency reason;
- failed entries preserve error reason;
- update proof transactions match `latestProof(assetId)`;
- trip transactions change circuit state as expected.

---

## 7. Evidence Matrix

| Asset ID | Property Label | Observed status | Expected hash | Actual hash | Planned action | Attestation digest | Signature present | Tx hash | Final state | Ghost-risk finding |
|---|---|---|---|---|---|---|---|---|---|---|
| 0x52aa9c8c3e83a0f1f4f73b1f4d0f2c4a4b3a2d1c0e9d8c7b6a5948372615040f | RealT Detroit Property #1 | mismatch | 0xe3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855 | 0x182991846b0591fc8b36580884d247afeb695bb9271ed7e53fd68e977f7be8ed | tripCircuit | Pending | Pending | Blocked | Blocked | GR-02 (test case) |
| 0x9f3e2a1b8c7d6e5f4a3b2c1d0e9f8a7b6c5d4e3f2a1b0c9d8e7f6a5b4c3d2e1f | RealT Detroit Property #2 | fresh | 0x182991846b0591fc8b36580884d247afeb695bb9271ed7e53fd68e977f7be8ed | 0x182991846b0591fc8b36580884d247afeb695bb9271ed7e53fd68e977f7be8ed | updateProof | Pending | Pending | Blocked | Blocked | None |

*Evidence matrix populated with current asset configuration. Full attestation and transaction data available after Phase 2 deployment.*

---

## 8. Issuer-Facing Findings Format

Each finding should be written in this form:

```text
Finding ID: GR-XX
Asset: [asset identifier / property label]
Severity: [Low / Medium / High / Critical]
Observed state: [what public data showed]
Expected state: [what registry or issuer metadata claimed]
Attested decision: [SafeKrypte digest + action]
Executed result: [tx hash or blocked]
Impact: [issuer, investor, or protocol exposure]
Recommendation: [specific remediation]
Evidence: [file path, digest, transaction hash]
```

---

## 9. Publication Readiness Status

Current Phase 2 status:

```text
DEPLOYMENT_READY = true
DEPLOYER_WALLET_FUNDED = pending
CREDENTIALS_CONFIGURED = false
```

Required conditions for Class B publication:

```text
Balance >= 0.05 POL on Polygon Amoy
PRIVATE_KEY configured in .env
ORACLE_ADDRESS configured in .env
POLYGONSCAN_API_KEY configured
```

Test wallet (for demonstration):

```text
Configured via .env.example template
```

This framework is publication-ready. On-chain evidence fields will be populated after deployment credentials are obtained and Phase 2 is completed.

---

## 10. Final Assessment Template

### Audit conclusion

```text
[PASS / PARTIAL / BLOCKED / FAIL]
```

### Summary

```text
ProofBridge Liner detected [N] assets, generated [N] signed attestations, broadcast [N] transactions, and identified [N] ghost-risk findings.
```

### Gate impact

```text
Phase 2 deployment is [open / blocked]. The limiting condition is [condition].
```

### Issuer implication

```text
The issuer can use this report to identify proof drift, missing evidence, stale records, and transaction-level response history before investor exposure expands.
```

---

## 11. Publication Timeline & Evidence

### Class A Publication (Ready Now)
This audit framework is complete and ready for Class A research publication alongside:
- CircuitBreaker.sol source code
- Foundry test suite (14/14 passing)
- Formal specification PDF
- GitHub repository

### Class B Publication Evidence (Next Sprint)
After wallet funding and Amoy deployment, the report will be updated with:

1. deployed `CircuitBreaker` address;
2. deploy transaction hash;
3. contract verification on PolygonScan;
4. live `submitter-attestations.json` digest list;
5. live `broadcast-receipts.json`;
6. E2E trip demo transaction (Property #1 mismatch);
7. final ghost-risk finding table;
8. populated evidence matrix with real transaction data.

### Post-Publication Evidence
- Phase 4: Multi-oracle quorum attestations
- Phase 5: Full E2E demo recordings
- Production: Real issuer property audits
